# Test Environment Variables
# Used by Vitest unit tests and Playwright E2E tests

# =============================================================================
# LOCAL SUPABASE CONFIGURATION (for tests)
# =============================================================================
# Tests use local Supabase instance
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# Supabase Local URLs (default ports)
NEXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH"
SUPABASE_SERVICE_ROLE_KEY="sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz"

# =============================================================================
# AI PROVIDER CONFIGURATION (Mocked for tests)
# =============================================================================
# Tests should mock AI calls, but these are here for integration tests
AI_HOST="http://localhost:1234/v1"
LLM_MODEL="test-model"
EMBEDDING_MODEL="test-embedding-model"

# Don't use OpenRouter in tests (would cost money)
# OPENROUTER_API_KEY should NOT be set in tests
