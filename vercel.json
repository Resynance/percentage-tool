{
  "buildCommand": "pnpm turbo run build --filter=@repo/fleet-app",
  "devCommand": "pnpm turbo run dev --filter=@repo/fleet-app",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/fleet/.next",
  "github": {
    "silent": true
  }
}
