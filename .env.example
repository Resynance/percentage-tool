# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Different configurations for different environments:
#
# Local Development (with Supabase local):
#   - Use .env.local (not this file)
#   - DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
#   - See LOCALDEV_QUICKSTART.md for setup
#
# Docker:
#   - Use .env.docker (not this file)
#   - DATABASE_URL is set in docker-compose.yml
#
# Production (Vercel + Supabase Cloud):
#   - Set in Vercel Environment Variables (not a file)
#   - DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
#
# This .env.example is a TEMPLATE ONLY - copy and modify for your needs
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================
# The system supports two providers: LM Studio (local) and OpenRouter (cloud).
# If OPENROUTER_API_KEY is set, OpenRouter will be used. Otherwise, LM Studio.

# -----------------------------------------------------------------------------
# OPTION 1: LM Studio (Local - Default)
# -----------------------------------------------------------------------------
# Point this to your local LM Studio server (usually port 1234)
AI_HOST="http://localhost:1234/v1"

# LLM Model Name (Must match the model loaded in LM Studio)
LLM_MODEL="meta-llama-3.1-8b-instruct"

# Embedding Model Name (Must match the embedding model loaded in LM Studio)
EMBEDDING_MODEL="text-embedding-nomic-embed-text-v1.5"

# -----------------------------------------------------------------------------
# OPTION 2: OpenRouter (Cloud)
# -----------------------------------------------------------------------------
# Get your API key from https://openrouter.ai/keys
# Setting this key will automatically switch to OpenRouter as the provider.
# Note: OpenRouter usage costs money. The app displays per-query costs and
# your remaining balance on the dashboard for cost visibility.
OPENROUTER_API_KEY="sk-or-v1-your-key-here"

# OpenRouter Base URL (default: https://openrouter.ai/api/v1)
OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"

# OpenRouter LLM Model (see https://openrouter.ai/models for options)
OPENROUTER_LLM_MODEL="gemini-3-flash-preview"

# OpenRouter Embedding Model
OPENROUTER_EMBEDDING_MODEL="openai/text-embedding-3-small"

# Optional: App identification for OpenRouter analytics
# OPENROUTER_REFERER="http://localhost:3000"
# OPENROUTER_TITLE="Operations Tools"

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Required for authentication and database access via Supabase client SDK
#
# Local Development:
#   - Use .env.local with local Supabase URLs (http://127.0.0.1:54321)
#   - Keys are pre-configured for local development
#
# Production:
#   - Set in Vercel Environment Variables
#   - Get from your Supabase Cloud project settings
#
# Docker:
#   - Use .env.docker with mock keys
#   - Simplified auth without full Supabase features
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
SUPABASE_SERVICE_ROLE_KEY="sb_secret_..."  # Server-side only, NEVER expose to client

